function rollCall(firstNames) {
  for (let idx = 0; idx < firstNames.length; idx += 1) {
    console.log(firstNames[idx]);
  }
}

let fNames = ['Bob', 'Bill', 'Rich', 'Jim', 'Dan'];
rollCall(fNames);
